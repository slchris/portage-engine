# Portage Engine Client Configuration

# Portage Engine server URL
PORTAGE_ENGINE_URL=http://localhost:8080

# Binary package cache path
BINPKG_PATH=/var/cache/binpkgs

# Cloud provider settings
CLOUD_PROVIDER=gcp
CLOUD_REGION=us-central1
CLOUD_ZONE=us-central1-a
