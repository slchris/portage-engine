# Portage Engine Configuration Example
# Place this file at /etc/portage/portage-engine.conf

# Enable or disable the wrapper
enabled=false

# Server URL where Portage Engine server is running
server_url=http://localhost:8080

# Fallback to local build if binary package not available
# true: will compile locally if server doesn't have the package
# false: will fail if server doesn't have the package
fallback_local=true

# Timeout for server requests (seconds)
timeout_seconds=30

# Cache directory for downloaded binary packages
cache_dir=/var/cache/binpkgs
